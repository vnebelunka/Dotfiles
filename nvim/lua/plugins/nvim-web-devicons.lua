return {
  "nvim-tree/nvim-web-devicons",
  lazy = true, -- Only loads when another plugin needs an icon
  opts = {
    default = true, -- Use a default icon if one isn't found
    color_icons = true, -- Enable colored icons
  },
}
